---
import Layout from "@/layouts/Layout.astro";
import CalendarComponent from "@/components/Calendar.tsx";
import Features from "@/components/Features.astro";
---

<Layout title="Calendario - BeeTraveler">
    <main class="space-y-20">
        <section
            class="relative bg-gradient-to-r from-primary to-black py-16 sm:py-24 md:py-32 min-h-[500px] flex items-center"
        >
            <div
                class="max-w-7xl mx-auto px-4 sm:px-6 md:px-12 grid grid-cols-1 md:grid-cols-2 items-center gap-12"
            >
                <!-- Texto -->
                <div class="text-center md:text-left text-white">
                    <h1
                        class="text-4xl sm:text-5xl md:text-6xl font-extrabold leading-tight drop-shadow-md"
                    >
                        Agenda Cultural <br class="hidden md:block" /> México 2025
                    </h1>
                    <p
                        class="mt-6 text-lg sm:text-xl md:text-2xl text-white/90 max-w-xl mx-auto md:mx-0 leading-relaxed"
                    >
                        Descubre los próximos festivales, conciertos,
                        exposiciones y tradiciones en todo el país. Agrega
                        eventos a tu calendario y vive la experiencia cultural
                        como nunca antes.
                    </p>
                    <div
                        class="mt-8 sm:mt-10 flex flex-wrap justify-center md:justify-start gap-4 sm:gap-6"
                    >
                        <!-- Botón principal -->
                        <a
                            href="#calendario"
                            class="px-6 sm:px-10 py-3 sm:py-4 bg-white text-black font-bold rounded-lg shadow-lg hover:bg-gray-100 transition transform hover:scale-105 text-base sm:text-lg"
                        >
                            📅 Ver Calendario
                        </a>
                        <!-- Botón secundario -->
                        <a
                            href="/landing"
                            class="px-6 sm:px-10 py-3 sm:py-4 border-2 border-white text-white font-bold rounded-lg hover:bg-white hover:text-black transition transform hover:scale-105 text-base sm:text-lg"
                        >
                            🚀 Ir a la Landing
                        </a>
                    </div>
                </div>

                <!-- Imagen -->
                <div class="flex justify-center">
                    <img
                        src="public/hero.webp"
                        alt="Evento cultural en México"
                        class="rounded-2xl shadow-2xl max-h-[300px] sm:max-h-[400px] md:max-h-[480px] object-cover ring-4 ring-white/40 w-full md:w-auto"
                    />
                </div>
            </div>
        </section>

        <!-- 📅 Calendario -->
        <section
            id="calendario"
            class="relative py-20 bg-gradient-to-br from-green-50 via-white to-emerald-50 -mt-1"
        >
            <!-- Encabezado -->
            <div class="text-center mb-12">
                <h2
                    class="text-4xl md:text-5xl font-extrabold text-emerald-600 drop-shadow-sm"
                >
                    📅 Calendario Cultural 2025
                </h2>
                <p class="mt-3 text-lg text-gray-600 max-w-2xl mx-auto">
                    Explora festivales, conciertos, exposiciones y más. Haz clic
                    en una fecha para descubrir qué pasa ese día.
                </p>
            </div>

            <!-- Caja calendario -->
            <div class="max-w-5xl mx-auto px-4">
                <div
                    class="bg-white shadow-2xl rounded-2xl p-8 border-2 border-emerald-400 ring-1 ring-emerald-100"
                >
                    <CalendarComponent client:load />
                </div>
            </div>
        </section>

        <!-- ✨ Features -->
        <section class="relative py-16 sm:py-20 bg-gray-50 -mt-1">
          <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
            <h2 class="text-3xl sm:text-4xl font-extrabold text-center text-gray-800 mb-12">
              ✨ Lo que hace especial a BeeTraveler
            </h2>
            <Features />
          </div>
        </section>

        <!-- 🚀 CTA Landing -->
        <section
          class="relative text-center py-12 sm:py-16 md:py-20 bg-gradient-to-r from-green-400 to-green-600 text-white mx-2 sm:mx-4 overflow-hidden -mt-1"
        >
          <div class="max-w-3xl mx-auto px-4 relative z-10">
            <h2 class="text-2xl sm:text-3xl md:text-4xl font-bold mb-4">
              ¿Quieres más experiencias?
            </h2>
            <p class="mb-6 text-base sm:text-lg">
              Descubre todo lo que tenemos preparado para ti en nuestra landing especial.
            </p>
            <a
              href="/landing"
              class="inline-block px-6 sm:px-8 py-3 sm:py-4 bg-white text-green-600 font-semibold rounded-lg shadow-md hover:bg-green-50 transition transform hover:scale-105"
            >
              Ir a la Landing →
            </a>
          </div>

          <!-- Detalle decorativo -->
          <div class="absolute inset-0 bg-transparent pointer-events-none z-0">
            <div class="w-full h-full bg-[url('/pattern.svg')] bg-repeat opacity-10"></div>
          </div>

          <!-- SVG Wave hacia el footer -->
          <div class="absolute bottom-0 left-0 right-0 overflow-hidden leading-none">
            <svg
              class="relative block w-full h-12 text-primary"
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 1440 320"
              fill="currentColor"
            >
              <path d="M0,224L48,202.7C96,181,192,139,288,144C384,149,480,203,576,213.3C672,224,768,192,864,181.3C960,171,1056,181,1152,197.3C1248,213,1344,235,1392,245.3L1440,256L1440,320L0,320Z"/>
            </svg>
          </div>
        </section>
    </main>
</Layout>
