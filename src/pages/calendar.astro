---
import Layout from "@/layouts/Layout.astro";
import CalendarComponent from "@/components/Calendar.tsx";
import Features from "@/components/Features.astro";
import CalendarIcon from "@/icons/calendar.astro";
---

<Layout title="BeelFest - Agenda Cultural de México">
  <main class="space-y-20 mt-1">
    <!-- Hero -->
    <section
      class="relative bg-gradient-to-r from-[#B9FF66] to-black py-16 sm:py-24 md:py-32 min-h-[500px] flex items-center"
    >
      <div
        class="max-w-7xl mx-auto px-4 sm:px-6 md:px-12 grid grid-cols-1 md:grid-cols-2 items-center gap-12"
      >
        <!-- Texto -->
        <div class="text-center md:text-left text-white">
          <h1
            class="animate-fade-in text-4xl sm:text-5xl md:text-6xl font-extrabold leading-tight drop-shadow-md"
          >
            <strong>BeelFest</strong>
            <br />
            Donde cada evento <br class="hidden md:block" /> cuenta una historia.
          </h1>
          <p
            class="animate-slide-out-bottom mt-6 text-lg sm:text-xl md:text-2xl text-white/90 max-w-xl mx-auto md:mx-0 leading-relaxed"
          >
            Explora la <strong>Agenda Cultural de México 2025</strong>:
            festivales, conciertos, ferias, exposiciones y tradiciones que dan
            vida a cada rincón del país. Agrega eventos a tu calendario y sé
            parte de la herencia cultural que nos une.
          </p>
          <div
            class="mt-8 sm:mt-10 flex flex-wrap justify-center md:justify-start gap-4 sm:gap-6"
          >
            <!-- Botón principal -->
            <a
              href="#calendario"
              class="animate-zoom-in px-6 sm:px-10 py-3 sm:py-4 bg-white text-black font-bold rounded-lg shadow-lg hover:bg-gray-100 transition transform hover:scale-105 text-base sm:text-lg"
            >
              <div class="flex items-center gap-2">
                <CalendarIcon />
                <span>Ver calendario</span>
              </div>
            </a>
            <!-- Botón secundario -->
            <a
              href="/Landing"
              class="animate-zoom-in inline-block px-6 sm:px-8 py-3 sm:py-4 bg-white text-green-600 font-semibold rounded-lg shadow-md hover:bg-green-50 transition transform hover:scale-105"
            >
              Crear evento →
            </a>
          </div>
        </div>

        <!-- Imagen hero -->
        <div class="flex  justify-center">
          <img
            src="/calendar-hero.png"
            alt="Eventos culturales de México"
            class=" animate-scale rounded-2xl shadow-2xl max-h-[300px] sm:max-h-[400px] md:max-h-[480px] object-cover ring-4 ring-white/40 w-full md:w-auto"
          />
        </div>
      </div>
    </section>

    <!-- 📅 Calendario -->
    <section
      id="calendario"
      class="relative py-20 bg-gradient-to-br from-green-50 via-white to-emerald-50 -mt-1"
    >
      <div class="text-center mb-12">
        <h2
          class="text-4xl md:text-5xl font-extrabold text-emerald-600 drop-shadow-sm"
        >
          📅 Calendario Cultural de México 2025
        </h2>
        <p class="mt-3 text-lg text-gray-600 max-w-2xl mx-auto">
          Descubre los principales <strong
            >festivales culturales, ferias artesanales, exposiciones y
            celebraciones tradicionales</strong
          > en todo México. Selecciona una fecha para conocer los eventos disponibles
          en tu región.
        </p>
      </div>

      <div class="max-w-5xl mx-auto px-4">
        <div
          class="bg-white shadow-2xl rounded-2xl p-8 border-2 border-emerald-400 ring-1 ring-emerald-100"
        >
          <CalendarComponent client:load />
        </div>
      </div>
    </section>

    <!-- ✨ Features -->
    <section class="relative py-16 sm:py-20 bg-gray-50 -mt-1">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2
          class="text-3xl sm:text-4xl font-extrabold text-center text-gray-800 mb-12"
        >
          ✨ Lo que hace único a BeelFest
        </h2>
        <p class="text-center text-gray-600 mb-12 max-w-3xl mx-auto">
          BeelFest conecta cultura, tecnología y turismo sostenible. Más que un
          calendario, es una ventana digital al arte, las tradiciones y la
          identidad mexicana.
        </p>
        <Features />
      </div>
    </section>

    <!-- 🚀 CTA -->
    <section
      class="relative text-center py-12 sm:py-16 md:py-20 bg-gradient-to-r from-green-400 to-green-600 text-white mx-2 sm:mx-4 overflow-hidden -mt-1"
    >
      <div class="max-w-3xl mx-auto px-4 relative z-10">
        <h2 class="text-2xl sm:text-3xl md:text-4xl font-bold mb-4">
          ¿Listo para vivir México?
        </h2>
        <p class="mb-6 text-base sm:text-lg">
          Descubre experiencias auténticas, conecta con la cultura viva y
          celebra lo que nos une. Explora todo el ecosistema de BeelGo.
        </p>
        <a
          href="/Landing"
          class="inline-block px-6 sm:px-8 py-3 sm:py-4 bg-white text-green-600 font-semibold rounded-lg shadow-md hover:bg-green-50 transition transform hover:scale-105"
        >
          Ir a BeelGo →
        </a>
      </div>

      <div class="absolute inset-0 bg-transparent pointer-events-none z-0">
        <div
          class="w-full h-full bg-[url('/pattern.svg')] bg-repeat opacity-10"
        >
        </div>
      </div>

      <div
        class="absolute bottom-0 left-0 right-0 overflow-hidden leading-none"
      >
        <svg
          class="relative block w-full h-12 text-primary"
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 1440 320"
          fill="currentColor"
        >
          <path
            d="M0,224L48,202.7C96,181,192,139,288,144C384,149,480,203,576,213.3C672,224,768,192,864,181.3C960,171,1056,181,1152,197.3C1248,213,1344,235,1392,245.3L1440,256L1440,320L0,320Z"
          ></path>
        </svg>
      </div>
    </section>
  </main>
</Layout>
